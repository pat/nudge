!!! Strict
%html
  %head
    %title nudge
    %link{:href => '/css/reset.css', :type => 'text/css', :rel => 'stylesheet'}
    %link{:href => '/css/form.css', :type => 'text/css', :rel => 'stylesheet'}
    %link{:href => '/css/core.css', :type => 'text/css', :rel => 'stylesheet'}
  %body
    %h1 Nudge: Edit Site
    
    %form.formtastic{:action => "/update/#{@site.id}", :method => 'post'}
      %fieldset.inputs
        %ol
          %li.string
            %label{:for => 'name'} Name
            %input{:id => 'name', :name => 'name', :type => 'text', :value => @site.name}
            %p.inline-hints The name of the GitHub project.
          %li.string
            %label{:for => 'repo'} Repository
            %input{:id => 'repo', :name => 'repo', :type => 'text', :value => @site.repo}
            %p.inline-hints The git URL to clone from.
          %li.string
            %label{:for => 'folder'} Folder
            %input{:id => 'folder', :name => 'folder', :type => 'text', :value => @site.folder}
            %p.inline-hints The directory on the server to deploy to. Nudge will need permissions to this folder to deploy to it. It will try to create the folder if it doesn't already exist.
      %fieldset.buttons
        %ol
          %li.commit
            %input{:name => 'commit', :type => 'submit', :value => 'Save Changes'}

    %p
      %a{:href => '/'} Back